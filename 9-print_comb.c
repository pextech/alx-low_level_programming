#include <stdio.h>

/**
 * main - program that prints all possible combinations of numbers
 * Return: 0
 */

int main(void)
{
int num = 48;
int keep = 44;
while (num <= 57)
{
putchar(num);
if (n != 57)
{
putchar(keep);
putchar(32);
}
num += 1;
}
putchar('\n');
return (0);
}
