#include "holberton.h"

/**
 * add - adds two integers and returns the result
 * @n: added to m
 * @m: added to n
 *
 * Return: returns result of addition of two integers
 */
int add(int n, int m)
{
int sum;

sum = n + m;
return (sum);
}
